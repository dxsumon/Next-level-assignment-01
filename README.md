#। TypeScript-এ Interface এবং Type এর মধ্যে পার্থক্য নিচে দেওয়া হলো!!

Infterface:
১। Interface মূলত ব্যবহার করা হয় Object shape দেয়ার জন্য । Interface শুধু Object type এর উপর কাজ করে, Interface কাজ করবে না Primitive type like string, number, or other non object type এর উপর ।
২। আমরা যদি একি নামে দুইটা Interface declare করি তাহলে একটা আরেকটার সাথে মার্জ করবে মানে আগের Type এর সাথে নতুন Type যুক্ত হয়ে যাবে। 
৩। Interface একটা থেকে আরেকটা খুব সহজেই Extend করা যায় এবং এটি Class implement করতে পারে।
৪। Syntax ও ভিন্নঃ
interface Person { 
    name: string; 
    age: number; 
}

Type Alias:
১। যেকোন ধরনের টাইপ like object, union, tuple, primitive type যেমন string, number, boolean সব কিছুতেই ব্যবহার করা যাবে।
২। না, type alias merge করা যায় না Error দিবে।
৩।type alias extend করতে পারবে না সরাসরি, তবে intersection (&) ব্যবহার করে Extend করে।
৪। Syntax ও ভিন্নঃ
type Person = { 
    name: string;
    age: number; 
}


#। Any, unknown, এবং never টাইপের পার্থক্য Explain করা হলো

*। Any:
১। সবকিছু গ্রহণ করে, কোন টাইপ চেক করে না।

*।Unknown
১।


*। Never:
১।


# **TypeScript: Interface vs Type & Special Types (any, unknown, never)**

## **📌 Interface এবং Type এর পার্থক্য**

### **🔷 Interface**

১। Interface মূলত ব্যবহার করা হয় **Object shape** দেয়ার জন্য । Interface শুধু Object type এর উপর কাজ করে, Interface ব্যবহার করা যায় না Primitive type like string, number, or other non object type এর উপর ।
২। একই নামে একাধিক Interface লিখলে তারা merge হয়ে যায়, আমরা যদি একি নামে দুইটা **Interface declare** করি তাহলে একটা আরেকটার সাথে মার্জ করবে মানে আগের Type এর সাথে নতুন Type যুক্ত হয়ে যাবে। 
3. Interface সহজেই **extend** করা যায় এবং এটি **class implement** করতে পারে যেটা অনেক **Powerfull** একটা Feature।
4. **Syntax:**

```ts
interface Person {
  name: string;
  age: number;
}
```

### **🔶 Type Alias**

1. Type alias ব্যবহার করা যায় **object, union, tuple, primitive type** ছাড়াও আরও সব কিছুর জন্য।
2. Type alias Interface এর মতো **merge সাপোর্ট করে না** একই নামে আবার type দিলে error হবে।
3. Type alias **extend করা যায় না সরাসরি**, তবে **intersection (&)** ব্যবহার করে করা যায়।
4. **Syntax:**

```ts
type Person = {
  name: string;
  age: number;
}
```

---

## **Any, unknown, এবং never টাইপের পার্থক্য Explain করা হলো**

### **🔴 Any**

* Any টাইপ **যেকোনো টাইপের মান গ্রহণ করতে পারে** কোন চেকিং করতে হয় না।
* এটি TypeScript এর **type checking সম্পূর্ণরূপে বন্ধ করে দেয়**।
* ভুল Type pass করলেও Error দিবে না, তাই এটি খুবই **unsafe**।

### **🟡 Unknown**

* Unknown টাইপ **সবকিছু hold করতে পারে**, কিন্তু সরাসরি ব্যবহার করা যায় না।
* ব্যবহার করার আগে **type check করা বাধ্যতামূলক**, তাই এটি much safer than `any`।

### **Example**

```ts
let data: unknown = "Hello";

if (typeof data === "string") {
  console.log(data.toUpperCase());
}
```


### **⚫ Never**

* Never টাইপ এমন কিছুকে বোঝায় যা **কখনো ঘটে না**।
* যেসব ফাংশন **return করে না** (infinite loop বা error throw করে) তাদের return type হয় `never`।

### **Example**

```ts
function throwError(message: string): never {
  throw new Error(message);
}
```